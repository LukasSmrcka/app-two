spring:
  application:
     name: app-two
  profiles:
    #active: primary

  cloud:
    config:
      uri: http://localhost:8002
      # Application will not start without configuration server
      failFast: true